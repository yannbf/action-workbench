name: Test

on:
  pull_request:
    types: [labeled]
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.label.name }}
      - run: echo ${{ toJson(github.event.label) }}
  test2:
    runs-on: ubuntu-latest
    if: github.event.label
    steps:
      - run: echo ${{ github.event.label }}
  test3:
    runs-on: ubuntu-latest
    if: ${{ github.event.label != ''}}
    steps:
      - run: echo ${{ github.event.label }}